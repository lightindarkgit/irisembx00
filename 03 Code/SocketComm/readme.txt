说明：

1、socketCon类继承自INetSocket，用来初步的实现类接口呈现的利用多态实现回调函数的绑定。

2、socketBase是所有网络通信的抽象基类，不断完善中。目前尚未确定在何处使用，初步考虑是在客户端与服务端处理的工厂中使用。

3、socketAPI用于对数据的对外接口，封装客户端或者是服务端。

4、在客户端和服务端两个文件夹中，通过继承基类实现不同种的网络通信框架，目前主要提供select、poll、epoll，争取再提供一个libevent的框架。对epoll中的惊群和非阻塞注意一下。
